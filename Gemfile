source :rubygems

gemspec

group :development do
  # since there is no easy way to express this in the gemspec
  # declare them here instead
  gem 'rcov',      :platform => :ruby_18
  gem 'simplecov', :platform => :ruby_19
end
